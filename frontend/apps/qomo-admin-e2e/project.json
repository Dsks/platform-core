{
  "name": "qomo-admin-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/qomo-admin-e2e/src",
  "implicitDependencies": ["qomo-admin"],
  "// targets": "to see all targets run: nx show project qomo-admin-e2e --web",
  "targets": {
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "outputs": ["{workspaceRoot}/dist/.playwright/apps/qomo-admin-e2e"],
      "options": {
        "config": "apps/qomo-admin-e2e/playwright.config.ts"
      }
    }
  }
}
